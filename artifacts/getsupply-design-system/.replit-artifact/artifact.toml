kind = "design-system"
previewPath = "/getsupply-design-system/"
title = "GetSupply Design System"
description = "Reusable design system foundation for the GetSupply product experience."
version = "1.0.0"
id = "artifacts/getsupply-design-system"
router = "path"

[[services]]
name = "web"
paths = [ "/getsupply-design-system/" ]
localPort = 21195

[services.development]
run = "pnpm --filter @workspace/getsupply-design-system run dev"

[services.env]
PORT = "21195"
BASE_PATH = "/getsupply-design-system/"
